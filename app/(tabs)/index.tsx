import React from "react";
import { Text, View } from "react-native";

const App = () => {
	return (
		<View className="flex-1 justify-center items-center">
			<Text className="text-3xl font-semibold text-dark-200">Welcome!</Text>
		</View>
	);
};

export default App;
